import {
   
} from "../types";


const initialState = {
};

const Menu = (state = initialState, { type, payload }) => {
    switch (type) {
        default: {
            return state;
        }
    }
};

export default Menu;
